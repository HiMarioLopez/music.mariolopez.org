{
    "name": "get-schedule-rate",
    "version": "1.0.0",
    "description": "Lambda function to get schedule rate",
    "main": "dist/index.js",
    "scripts": {
        "build": "esbuild index.ts --bundle --minify --platform=node --target=node22 --outfile=dist/index.js"
    },
    "dependencies": {
        "@aws-sdk/client-ssm": "^3.759.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.147",
        "@types/node": "^22.13.10",
        "esbuild": "^0.25.1",
        "typescript": "^5.8.2"
    }
}